### オブジェクト

```javascript
// オブジェクト
const object = { id: 1, name: 'taro' };

// オブジェクトの値を取得
console.log(object[id]); // 1
console.log(object.id); // 1

// プロパティの追加
object['age'] = 20;
console.log(object);
```
